/**
 * Solid Pattern
 * No pattern overlay - just the base shape colors
 */

/**
 * Generate solid pattern (no overlay)
 * @param {Object} colors - Color scheme
 * @param {Object} patternArea - Area to apply pattern to
 * @param {Object} options - Pattern options
 * @returns {string} Empty string (no pattern elements)
 */
export function solidPattern(colors, patternArea, options = {}) {
  // Solid pattern has no overlay
  return "";
}
